<?php

/**
 * Exercício 5 — Loops
 *
 * Objetivo: praticar for, while, do...while, foreach, break e continue.
 *
 * TODO 1: Use um for para imprimir os números de 1 a 20, mas pulando os
 *         múltiplos de 3 (continue) e parando se chegar em 18 (break).
 *
 * TODO 2: Use while para simular uma "tentativa de senha": uma variável guarda
 *         a senha correta, e o loop simula tentativas (pode ser um array fixo
 *         de tentativas, já que ainda não vimos input do usuário) até acertar
 *         ou esgotar as tentativas.
 *
 * TODO 3: Use do...while para garantir que um bloco rode pelo menos uma vez,
 *         mesmo quando a condição já começa falsa. Escreva um exemplo onde
 *         isso faz diferença comparado a um while comum.
 *
 * TODO 4: Use foreach para percorrer o array multidimensional de pessoas do
 *         exercício 03-arrays.php (pode recriá-lo aqui) e imprimir só as
 *         pessoas com idade maior que 30.
 */

// seu código aqui
