<?php

/**
 * Exercício 4 — Condicionais (if/else, switch, match, ??, ?->)
 *
 * Objetivo: praticar as formas "clássicas" e as formas modernas (PHP 8) de
 * condicional, e entender quando cada uma faz mais sentido.
 *
 * TODO 1: Escreva uma função (pode ser um bloco de código direto, funções
 *         vêm no Módulo 02) que recebe uma nota (0 a 10) e imprime o conceito:
 *         >= 9: "A", >= 7: "B", >= 5: "C", senão "D".
 *         Faça uma versão com if/else e outra com match (usando faixas via
 *         match(true) { $nota >= 9 => "A", ... }).
 *
 * TODO 2: Crie um array associativo que pode ou não ter a chave "apelido".
 *         Use o operador ?? para definir um valor padrão quando a chave não
 *         existir. Teste os dois casos (com e sem a chave).
 *
 * TODO 3: Simule um "objeto" que pode ser null (dica: uma variável que às
 *         vezes é um array associativo com um método fictício representado
 *         por uma função, às vezes é null) e use o operador ?-> me explicando
 *         em comentário por que ele evita um erro fatal. Se preferir, pesquise
 *         um exemplo simples com stdClass para isso.
 *
 * TODO 4: Compare, em um comentário, switch tradicional vs match: o que muda
 *         no tipo de comparação (== vs ===) e na obrigatoriedade do break.
 */

// seu código aqui
